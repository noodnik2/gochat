// Package chatter contains adapters for interacting with chat models.
//
// Each chatter adapter should have a constructor, and configuration structure which
// can be directly embedded into the application's configuration, and support the
// common port interfaces needed by the application service layer.
package chatter
